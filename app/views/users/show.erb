<h2><%=@user.username.capitalize%>'s BrainFood</h2>

<% @user.shelves.each do |shelf| %>
  <h3> <%=shelf.name%> </h3>
    <% shelf.books.each do |book| %>
      <ul>
        <li> <%= book.title %> by <%= book.author %>
      </ul>
    <% end %>
  <% end %>    